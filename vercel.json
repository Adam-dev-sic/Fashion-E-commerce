{
  "version": 3,
  "builds": [
    { "src": "client/package.json", "use": "@vercel/vite" },
    { "src": "server/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server/$1.js" },
    { "src": "/(.*)", "dest": "client/$1" }
  ]
}
